---
title: "Run TypeScript contract tests against a forked EVM"
sidebarTitle: "test"
description: "Execute TypeScript describe and it tests from the tests directory against a forked EVM, with optional coverage output and per-function gas reporting."
---

Executes tests defined in `tests/` against a forked EVM. Tests are plain TypeScript with a `describe` / `it` API.

## Usage

```bash
npx scriipture test [pattern] [options]
```

## Options

| Flag | Description |
| --- | --- |
| `--fork <chain>` | Fork a specific chain for tests. |
| `--coverage` | Emit a coverage report. |
| `--gas` | Emit per-function gas usage. |

## Example

```bash
npx scriipture test MyToken --gas
```